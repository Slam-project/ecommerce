{% extends "::base.html.twig" %}

{% block body %}
    <div class="container">
        <h4>{{ product.name }}</h4>
        {{ product.description }}
        {{ form(panier) }}
    </div>
{% endblock %}
